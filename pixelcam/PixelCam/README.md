## PixelCam（Xcode 工程已建好）

直接用 Xcode 打开 `PixelCam.xcodeproj` 即可。

### 运行前必做
1. 打开工程后，在 Target → Signing & Capabilities 里把 **Team** 选成你的 Apple ID 团队（自动签名）
2. 真机运行（相机需要真机）

### 说明
- 已包含相机权限与保存到相册权限文案（在 `PixelCam/Info.plist`）
- 功能：实时 1-bit 黑白预览、3 预设（Clean/Grit/Poster）、Threshold、拍照结果页、保存/分享

